/* Variables: es una forma de declarar el valor de algo que va ser usado más tarde. Se declara con la palabra clave var y ahora saliendo otras que son: let (nivel de scope) y const (variable que no se puede cambiar, es constante)

Strings: bloques de textos entre comillas

Funciones (argumentos, return): las funciones son objetos invocables y se establece el nombre de la función entre paréntesis los argumentos (que serían las variables que le establecemos) y entre llaves el código return para que nos entregue el resultado que queremos, es decir que la función determine lo que está haciendo y te devuelve lo especificado. 

Declaraciones if: es un operador de control de flujo y comparación, declaración que se usa para que la función verifique si es algo es true ejecutando un código que indique si es así o avanzando si no lo es. 

Valores booleanos (true, false):  Indican verdadero o falso
 */